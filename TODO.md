# ft_linear_regression TODOリスト

> WASM/IPFSのような固有名詞の技術がほぼ登場しません。外部ライブラリはむしろ「使いすぎると減点」される側で、主役は手書きの勾配降下法ループと、それを収束させるための地味な工夫(同時更新・学習率選定・特徴量スケーリング)そのものです。インフラや配線ではなく、数式を正しくコードに落とし込めるかどうかが問われる、小さく閉じた課題です。

課題文に明示的な実装順序の指定はない。ただし2つ目のプログラム(学習)が吐き出す`theta0`/`theta1`を1つ目のプログラム(予測)が読み込む依存関係があるため、**予測プログラムの骨組み→学習プログラム→統合確認**の順で進めるのが自然。

## フェーズ0: 環境・技術選定

- [x] 実装言語を決める → Python
- [ ] 使ってよい/使ってはいけないライブラリの境界を確認する(`numpy.polyfit`のような「代わりに全部解いてくれる」関数は禁止。行列演算などの低レベルなライブラリは可)
- [x] 訓練用データセット(mileage, price の組のCSV)を用意する → `data.csv` をプロジェクトルートに配置済み
- [ ] θ0/θ1の永続化フォーマットを決める(CSV/JSON/プレーンテキストなど、仕様書に指定なし)

## フェーズ1: 予測プログラム

- [ ] 起動時にユーザーへ走行距離(mileage)の入力を求める処理を実装する
- [ ] 仮説関数 `estimatePrice(mileage) = theta0 + theta1 * mileage` を実装する
- [ ] 学習プログラムが保存したθ0/θ1をファイルから読み込む処理を実装する
- [ ] パラメータファイルが存在しない場合、theta0=0, theta1=0にフォールバックする処理を実装する
- [ ] 推定価格を表示する処理を実装する

## フェーズ2: 学習プログラム

- [ ] データセットファイル(CSV)を読み込むパース処理を実装する
- [ ] コスト計算に使う `estimatePrice` を予測プログラムと共通のロジックとして実装する(重複を避ける)
- [ ] mileage/priceの特徴量スケーリング(min-maxスケーリングなど)を実装する(正規化なしでは学習率次第で発散しやすいため)
- [ ] 学習率(learning rate)を決め、ハイパーパラメータとして設定できるようにする
- [ ] tmp_theta0の更新式 `learningRate * (1/m) * Σ(estimatePrice(mileage[i]) - price[i])` を実装する
- [ ] tmp_theta1の更新式 `learningRate * (1/m) * Σ(estimatePrice(mileage[i]) - price[i]) * mileage[i]` を実装する
- [ ] theta0とtheta1を**同時に**更新する処理を実装する(片方の更新に、もう片方の新しい値を使ってしまうミスを避ける)
- [ ] 収束判定(固定イテレーション回数、またはコストの変化量による打ち切り)を実装する
- [ ] スケーリングした空間で学習したthetaを、元のmileage/priceのスケールに戻す処理を実装する(スケーリングを行った場合)
- [ ] 学習後のtheta0/theta1をファイルに保存する処理を実装する

## フェーズ3: 統合・検証

- [ ] 学習前(theta0=theta1=0)の状態で予測プログラムを実行し、常に価格0が返ることを確認する
- [ ] 学習プログラムを実行し、thetaが保存されることを確認する
- [ ] 学習後に予測プログラムを実行し、走行距離に応じて妥当な価格が返ることを確認する
- [ ] 学習率を極端に大きくして発散するケース、極端に小さくして収束が遅いケースを試し、選んだ学習率の妥当性を確認する

## フェーズ4: 提出準備

- [ ] リポジトリのフォルダ名・ファイル名が課題の指定通りか確認する
- [ ] 指定された仮説関数(estimatePrice)がそのまま使われているか確認する
- [ ] 指定された学習式(同時更新含む)がそのまま使われているか確認する
- [ ] 代わりに作業をしてくれるライブラリ(`numpy.polyfit`など)を使っていないか最終確認する

## フェーズ5(任意・必須部分が完璧な場合のみ): ボーナス

- [ ] データセットを散布図としてプロットするプログラム(または機能)を追加する
- [ ] 学習で得られた回帰直線を、同じグラフ上にデータと重ねてプロットする
- [ ] アルゴリズムの精度(R²やMSEなど)を計算するプログラムを追加する

---

**進め方の目安**: 予測プログラムは仮説関数が単純なので先に骨組みだけ作り、学習プログラム(フェーズ2)に時間の大半を使う。特にスケーリングと同時更新は動作確認(フェーズ3)で発散・停滞が起きた際に真っ先に疑う箇所。
