# ft_linear_regression TODOリスト

> WASM/IPFSのような固有名詞の技術がほぼ登場しません。外部ライブラリはむしろ「使いすぎると減点」される側で、主役は手書きの勾配降下法ループと、それを収束させるための地味な工夫(同時更新・学習率選定・特徴量スケーリング)そのものです。インフラや配線ではなく、数式を正しくコードに落とし込めるかどうかが問われる、小さく閉じた課題です。

課題文に明示的な実装順序の指定はない。ただし2つ目のプログラム(学習)が吐き出す`theta0`/`theta1`を1つ目のプログラム(予測)が読み込む依存関係があるため、**予測プログラムの骨組み→学習プログラム→統合確認**の順で進めるのが自然。

## フェーズ0: 環境・技術選定

- [x] 実装言語を決める → Python
- [x] 使ってよい/使ってはいけないライブラリの境界を確認する → `estimatePrice`/学習ループ/平均・標準偏差はすべて自前実装。`statistics`ライブラリも使わない方針に統一
- [x] 訓練用データセット(mileage, price の組のCSV)を用意する → `data.csv` をプロジェクトルートに配置済み
- [x] θ0/θ1の永続化フォーマットを決める → `theta.txt` にカンマ区切りのプレーンテキストで保存

## フェーズ1: 予測プログラム

- [x] 起動時にユーザーへ走行距離(mileage)の入力を求める処理を実装する → `predict.py`
- [x] 仮説関数 `estimatePrice(mileage) = theta0 + theta1 * mileage` を実装する
- [x] 学習プログラムが保存したθ0/θ1をファイルから読み込む処理を実装する → `load_theta`
- [x] パラメータファイルが存在しない場合、theta0=0, theta1=0にフォールバックする処理を実装する
- [x] 推定価格を表示する処理を実装する

## フェーズ2: 学習プログラム

- [x] データセットファイル(CSV)を読み込むパース処理を実装する → `load_data`
- [x] コスト計算に使う `estimatePrice` を予測プログラムと共通のロジックとして実装する(重複を避ける) → `predict.py`からimport
- [x] mileage/priceの特徴量スケーリングを実装する → z-score標準化(`standardize`)を採用。min-max版も一度実装して比較し、外れ値耐性の観点でz-scoreを選択
- [x] 学習率(learning rate)を決め、ハイパーパラメータとして設定できるようにする → `learning_rate=0.1`
- [x] tmp_theta0の更新式 `learningRate * (1/m) * Σ(estimatePrice(mileage[i]) - price[i])` を実装する
- [x] tmp_theta1の更新式 `learningRate * (1/m) * Σ(estimatePrice(mileage[i]) - price[i]) * mileage[i]` を実装する
- [x] theta0とtheta1を**同時に**更新する処理を実装する(片方の更新に、もう片方の新しい値を使ってしまうミスを避ける)
- [x] 収束判定を実装する → 固定イテレーション回数(1000回)
- [x] スケーリングした空間で学習したthetaを、元のmileage/priceのスケールに戻す処理を実装する → `unscale_theta`
- [x] 学習後のtheta0/theta1をファイルに保存する処理を実装する

## フェーズ3: 統合・検証

- [x] 学習前(theta.txtが存在しない状態)で予測プログラムを実行し、常に価格0が返ることを確認する
- [x] 学習プログラムを実行し、thetaが保存されることを確認する
- [x] 学習後に予測プログラムを実行し、走行距離に応じて妥当な価格が返ることを確認する
- [x] 正規化なし(生のmileage)で発散すること、正規化ありで収束することを実際に確認した(学習率0.1固定)

## フェーズ4: 提出準備

- [x] リポジトリのフォルダ名・ファイル名が課題の指定通りか確認する → 課題文に具体的な指定なし。役割がそのまま名前になっている現状のファイル名(predict/train/plot/evaluate)で問題なしと判断
- [x] 指定された仮説関数(estimatePrice)がそのまま使われているか確認する → `predict.py`の`estimate_price`を全ファイルがimportして使用、独自実装なし
- [x] 指定された学習式(同時更新含む)がそのまま使われているか確認する → `tmp_theta0`/`tmp_theta1`とも仕様書の式通り、`errors`を先に計算してから両方を更新しているため同時更新も担保
- [x] 代わりに作業をしてくれるライブラリ(`numpy.polyfit`など)を使っていないか最終確認する → 使用ライブラリは`csv`(パースのみ)と`matplotlib`(描画のみ)だけ、回帰計算は全て自前実装
- [x] 学習率とiterationの決め方を調査する → z-score正規化後の空間で実験し、発散境界が学習率≈2.0付近であることを確認。現在の`learning_rate=0.1`は境界の1/20、`iterations=1000`は収束に必要な126回の8倍で、安全マージンを持って妥当と判断
- [ ] レビューの説明時にこのモデルのわかりやすいデータを入れる

## フェーズ5(任意・必須部分が完璧な場合のみ): ボーナス

- [x] データセットを散布図としてプロットするプログラム(または機能)を追加する → `plot.py`
- [x] 学習で得られた回帰直線を、同じグラフ上にデータと重ねてプロットする
- [x] アルゴリズムの精度(R²やMSEなど)を計算するプログラムを追加する → `evaluate.py`

---

**進め方の目安**: 予測プログラムは仮説関数が単純なので先に骨組みだけ作り、学習プログラム(フェーズ2)に時間の大半を使う。特にスケーリングと同時更新は動作確認(フェーズ3)で発散・停滞が起きた際に真っ先に疑う箇所。
